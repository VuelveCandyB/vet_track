{
  "max_file_size_context_warning": false,
  "ignore_patterns": [
    ".agents/**/*.md",
    "node_modules/**",
    ".next/**",
    "dist/**"
  ],
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; if [[ $f =~ \\.(ts|tsx|js|jsx|ts|test)$ ]]; then npm test -- --testPathPattern=\"$(basename $f)\" 2>&1 | head -20; fi; } 2>/dev/null || true",
            "timeout": 45,
            "statusMessage": "Running tests for modified file...",
            "async": true
          }
        ]
      }
    ]
  }
}
