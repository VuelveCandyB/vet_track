describe('Utils', () => {
  it('should pass a basic test', () => {
    expect(true).toBe(true)
  })

  it('should add numbers', () => {
    expect(1 + 1).toBe(2)
  })

  it('hook test - should multiply numbers', () => {
    expect(5 * 2).toBe(10)
  })
})
